# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RMFlickr' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  platform :ios, '8.0'
  pod 'Alamofire', '~> 4.7'
  pod 'SDWebImage', '~> 4.0'
  # Pods for RMFlickr

  target 'RMFlickrTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RMFlickrUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
